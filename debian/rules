#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh ${@} --with python3

override_dh_auto_build:
	# Generate bash completion file
	mkdir -p doc/bash_completion.d
	doc/generate_bash_completion.py -o doc/bash_completion.d/yunohost
	# Generate zsh completion file
	mkdir -p doc/zsh_completion.d
	doc/generate_zsh_completion.py -o doc/zsh_completion.d/_yunohost
	# Generate man pages
	doc/generate_manpages.py --gzip --output doc/yunohost.8.gz
	po/generate_mo --output po/generated
